[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Variáveis de ambiente para o build
[build.environment]
  NODE_VERSION = "20"

# SPA: redireciona todas as rotas para index.html (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
